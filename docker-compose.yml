version: "3.8"

services:
  apollo-music-db:
    image: "mariadb:10.6"
    container_name: "apollo-music-db"
    restart: "always"
    environment:
      MARIADB_ROOT_PASSWORD: "hello"
      MARIADB_DATABASE: "apollo-music"
    ports:
      - "3306"
    volumes:
      - db-config:/etc/mysql
      - db-data:/var/lib/mysql
      # - ./server/data/db/init.sql:/docker-entrypoint-initdb.d
    networks:
      - apollo-music

  apollo-music-app:
    build:
      dockerfile: Dockerfile
      context: "./server"
    container_name: "apollo-music-app"
    depends_on:
      - apollo-music-db
    # restart: "unless-stopped"
    restart: "always"
    ports:
      - "8080:8080"
    stdin_open: true
    tty: true
    networks:
      - apollo-music

  apollo-music-client:
    build:
      dockerfile: Dockerfile
      context: "./client"
    depends_on:
      - apollo-music-app
    # restart: "unless-stopped"
    restart: "always"
    ports:
      - "8081:3000"
    stdin_open: true
    tty: true

volumes:
  db-config:
  db-data:

networks:
  apollo-music: {}
